export default function Components(props) {
    return (
        <>

            <section>{props.sec}</section>
            <strong>{props.negrito}</strong>
            <p>{props.paragrafo}</p>
            <section>{props.anuncio}</section>
        </>
    )

}