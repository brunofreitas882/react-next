export default function titulo(props) {
    return (
        <>

            <h1>{props.titulo}</h1>
            <h3>{props.subtitulo}</h3>
            <p>{props.paragrafo}</p>
            <section>{props.anuncio}</section>
        </>
    )

}